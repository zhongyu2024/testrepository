<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pinhaha.mapper.AppAccountLogMapper">
	<select id="getAllBill" parameterType="Map" resultType="AppAccountLog">
		select * from app_account_log
		where 1=1
		<if test="date1!=null and date1!=''">
			and create_time between #{date1} and #{date2}
		</if>
		<if test="user_id!=null and user_id!=''">
			and user_id=#{user_id}
		</if>
		<if test="change_amount!= null and change_amount!=''">
			and change_amount=#{change_amount}
		</if>
		order by id desc limit #{page},#{limit}
	</select>
	
	<select id="getAllBillCount" parameterType="Map" resultType="int">
		select count(1) from app_account_log
		where 1=1
		<if test="date1!=null and date1!=''">
			and create_time between #{date1} and #{date2}
		</if>
		<if test="user_id!=null and user_id!=''">
			and user_id=#{user_id}
		</if>
		<if test="change_amount!= null and change_amount!=''">
			and change_amount=#{change_amount}
		</if>
	</select>
</mapper>