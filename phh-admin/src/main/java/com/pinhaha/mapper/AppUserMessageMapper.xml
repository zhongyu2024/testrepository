<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pinhaha.mapper.AppUserMessageMapper">
	<select id="getMessageList" resultType="com.pinhaha.pojo.AppUserMessage">
		select * from app_user_message where user_id=#{userId} order by id desc
	</select>
	
	<select id="unReadMessageCout" resultType="java.lang.Integer">
		select count(1) from app_user_message where user_id=#{userId} and is_read=0
	</select>
</mapper>